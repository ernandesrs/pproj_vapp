export interface SkeletonProps {
    show?: boolean,
    circle?: boolean,
    customTailwindSizeClass?: string
};

export interface SkeletonTextProps {
    show?: boolean,
    lines?: number,
    lineHeight?: string,
    lineGap?: string
};